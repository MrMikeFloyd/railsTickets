<%= form_with(model: claim_status, local: true) do |form| %>
  <% if claim_status.errors.any? %>
    <div id="error_explanation">
      <h2><%= pluralize(claim_status.errors.count, "error") %> prohibited this claim_status from being saved:</h2>

      <ul>
      <% claim_status.errors.full_messages.each do |message| %>
        <li><%= message %></li>
      <% end %>
      </ul>
    </div>
  <% end %>

  <div class="field">
    <%= form.label :name %>
    <%= form.text_field :name, id: :claim_status_name %>
  </div>

  <div class="field">
    <%= form.label :description %>
    <%= form.text_field :description, id: :claim_status_description %>
  </div>

  <div class="actions">
    <%= form.submit %>
  </div>
<% end %>
