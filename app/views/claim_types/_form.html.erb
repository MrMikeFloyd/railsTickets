<%= form_with(model: claim_type, local: true) do |form| %>
  <% if claim_type.errors.any? %>
    <div id="error_explanation">
      <h2><%= pluralize(claim_type.errors.count, "error") %> prohibited this claim_type from being saved:</h2>

      <ul>
      <% claim_type.errors.full_messages.each do |message| %>
        <li><%= message %></li>
      <% end %>
      </ul>
    </div>
  <% end %>

  <div class="field">
    <%= form.label :name %>
    <%= form.text_field :name, id: :claim_type_name %>
  </div>

  <div class="field">
    <%= form.label :description %>
    <%= form.text_field :description, id: :claim_type_description %>
  </div>

  <div class="actions">
    <%= form.submit %>
  </div>
<% end %>
